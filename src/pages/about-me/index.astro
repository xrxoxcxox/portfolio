---
import GlobalLayout from "../../layouts/GlobalLayout.astro";
import { getEntry } from "astro:content";
const aboutMe = await getEntry("about-me", "index");
const { Content } = await aboutMe.render();
---

<GlobalLayout>
  <h1>{aboutMe.data.title}</h1>
  <Content />
</GlobalLayout>
